meta {
  name: list
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/deploy/list?status=running
  body: none
  auth: bearer
}

params:query {
  status: running
}

auth:bearer {
  token: {{DEEPINFRA_API_KEY}}
}
